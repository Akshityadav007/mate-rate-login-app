name: materateloginapp
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^0.5.1
  firebase_auth: ^0.18.2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  uses-material-design: true
